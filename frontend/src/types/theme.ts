export interface Theme {
  colors: {
    primary: string;
    secondary: string;
    accent: string;
    background: string;
    card: string;
    text: string;
    input: string;
    border: string;
    button: string;
    buttonText: string;
    error: string;
    navbarText: string;
    success: string;
    textLight: string;
  };
} 